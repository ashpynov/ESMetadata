<UserControl x:Class="ESMetadata.Views.MarkdownView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ctrl="clr-namespace:ESMetadata.Controls"
             xmlns:local="clr-namespace:ESMetadata.Views">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" Margin="0,10,0,10">
            <ctrl:HtmlTextView Margin="20,0,20,0"
                HorizontalAlignment="left"
                HtmlFontFamily="{DynamicResource FontFamily}"
                HtmlForeground="{DynamicResource TextColor}"
                LinkForeground="{DynamicResource GlyphColor}"
                HtmlFontSize="{DynamicResource FontSize}"
                HtmlText="{Binding HtmlContent}" />
        </ScrollViewer>

        <Button Content="{DynamicResource LOCCloseLabel}"
                HorizontalAlignment="Right"
                VerticalAlignment="Bottom"
                Margin="10"
                Click="CloseButton_Click"
                Grid.Row="1" />
    </Grid>
</UserControl>
